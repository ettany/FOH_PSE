# Redirect all non-API routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

# Redirect API requests to your backend
[[redirects]]
  from = "/api/*"
  to = "https://foh-pse.onrender.com/api/:splat"
  status = 200
  force = true
